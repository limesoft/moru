<% if user_signed_in? && topic.speaker_id == current_user.id && !(topic.speaker_id.present? && topic.event_id.present?) %>
	<%= link_to image_tag(topic.speaker.default_avatar)<<content_tag(:span, "сэдвийг ярина") ,unassign_topic_path(topic), class: "presenter", remote: true, method: :put %>
<% else %>
	 <%=content_tag(:span, link_to(image_tag(topic.speaker.default_avatar), profile_path(topic.speaker.id))<<content_tag(:span, "сэдвийг ярина"), class: "presenter")  %>
<% end %>
