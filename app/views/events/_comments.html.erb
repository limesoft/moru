<div class="comment-container">

  <%= render 'new_comment' if user_signed_in? %>

	<div class="comment-list" id="comment-list">
    <%= render partial: 'comment', collection: @comments %>
	</div>
</div>
<script>
  $(function(){
    infinity_pagination('<%= event_comments_path(event) %>', <%= @comments.num_pages %>);
  });
</script>
